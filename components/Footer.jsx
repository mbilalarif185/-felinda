import LuxeFooter from "@/components/luxe/sections/LuxeFooter";

/**
 * Site footer. Unified with the homepage — every page now shares the same
 * luxe dark footer (newsletter-adjacent links, socials, contact, legal).
 */
export default function Footer() {
  return <LuxeFooter />;
}
